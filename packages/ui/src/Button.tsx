import * as React from "react";
export const Button = () => {
  return (
    <button
      style={{
        fontSize: "3rem",
        color: "hotpink",
      }}
    >
      boop
    </button>
  );
};
